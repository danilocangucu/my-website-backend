export enum ProjectName {
  GOLDENRACK = "goldenrack",
  LOVETOKENS = "lovetokens",
}

export enum InstanceState {
  START = "start",
  STOP = "stop",
  TERMINATE = "terminate",
  STATUS = "status",
}
